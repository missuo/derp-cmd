name: Build and Release Derper

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get current date
        id: date
        run: echo "TODAY=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Install derper
        run: go install tailscale.com/cmd/derper@latest

      - name: Create build directory
        run: mkdir -p builds

      - name: Cross compile for different platforms
        run: |
          # Linux amd64
          GOOS=linux GOARCH=amd64 go build -o builds/derper-linux-amd64 tailscale.com/cmd/derper
          
          # Linux arm64
          GOOS=linux GOARCH=arm64 go build -o builds/derper-linux-arm64 tailscale.com/cmd/derper
          
          # Windows amd64
          GOOS=windows GOARCH=amd64 go build -o builds/derper-windows-amd64.exe tailscale.com/cmd/derper
          
          # macOS amd64
          GOOS=darwin GOARCH=amd64 go build -o builds/derper-darwin-amd64 tailscale.com/cmd/derper
          
          # macOS arm64
          GOOS=darwin GOARCH=arm64 go build -o builds/derper-darwin-arm64 tailscale.com/cmd/derper

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.date.outputs.TODAY }}
          name: Release ${{ steps.date.outputs.TODAY }}
          draft: false
          prerelease: false
          files: |
            builds/derper-linux-amd64
            builds/derper-linux-arm64
            builds/derper-windows-amd64.exe
            builds/derper-darwin-amd64
            builds/derper-darwin-arm64
